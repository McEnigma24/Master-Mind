#include <iostream>
#include <windows.h>
#include <time.h>
#include "headers.h"


using namespace std;

int main()
{
    srand(time(NULL));

    //Proste_Wyswietlanie(tab);

    //Wyswietl_Plansze(tab, feed_back);

    Gra();

    return 0;
}
